import { ChooserDirective } from './chooser.directive';

describe('ChooserDirective', () => {
  it('should create an instance', () => {
    const directive = new ChooserDirective();
    expect(directive).toBeTruthy();
  });
});
