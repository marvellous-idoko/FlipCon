import { PayserviceDirective } from './payservice.directive';

describe('PayserviceDirective', () => {
  it('should create an instance', () => {
    const directive = new PayserviceDirective();
    expect(directive).toBeTruthy();
  });
});
